export const metadata = {
    title: "Blogs",
    description: "Get the latest music, information, news and reviews from Nigerias best underground talents.",
  };
  
  export default function BlogLayout({ children }) {
    return (
        <div>
            {children}
        </div>
    );
  } 